<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fusion</title>
  <style>
    :root {
      --font-sans: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", "Noto Sans", "Liberation Sans", Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
      --font-size-base: 0.875rem;
      --font-size-small: 0.75rem;
      --font-size-large: 1.5rem;
      --line-height-base: 1.4;
      --color-link: #1E88E5;
      --color-dark: #212121;
      --color-light: #F5F5F5;
      --color-secondary: #757575;
    }

    html {
      font-family: var(--font-sans);
      font-size: var(--font-size-base);
      line-height: var(--line-height-base);
      color: var(--color-dark);
    }

    body {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 0.5em;
      min-height: 100dvh;
      min-height: 100vh;
    }

    header,
    main,
    footer {
      text-align: center;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    footer {
      margin-top: 2em;
    }

    h1 {
      font-size: var(--font-size-large);
    }

    a {
      color: var(--color-link);
    }

    p {
      margin-bottom: 0;
    }

    small {
      display: block;
      font-size: var(--font-size-small);
      color: var(--color-secondary);
      margin-bottom: 0.25rem;
    }

    code {
      padding: 0.125em 0.25em;
      font-size: var(--font-size-base);
      line-height: var(--line-height-base);
      background: var(--color-light);
      box-shadow: inset 0 0 0 1px rgba(0 0 0 / 25%);
      border-radius: 0.25em;
    }
  </style>
</head>

<body>
  <header>
    <img src="https://i.ibb.co/WtPCGfJ/Logo.png" alt="Logo" />
    <h1>{% block heading %}{% endblock %}</h1>
  </header>
  <main>
    {% block main %}{% endblock %}
  </main>
  <footer>
    {% if recipient %}
    <small>
      You are receiving this email because you have a Fusion account associated with
      <a href="mailto:mail@{{ recipient }}">{{ recipient }}</a>.
    </small>
    {% endif %}

    <small>&copy; Rapid Integration Team.</small>
  </footer>
</body>

</html>
